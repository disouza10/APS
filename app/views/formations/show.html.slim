.container.mb-4
  .row.text-center.mt-4.mb-4
    .col-md-12
      h1 = "Detalhes das formações do ano de #{@year}"

  .d-flex.flex-column
    - grouped = @report.group_by(&:team_name)
    - grouped.each do |team_name, volunteers|
      .row
        .card.mb-3
          .card-header
            h5 = team_name
          .card-body
            table.table.table-bordered
              thead
                tr
                  th Nome
                  th Email
                  th.text-center Presenças
              tbody
                - volunteers.each do |volunteer|
                  tr
                    td = volunteer.full_name
                    td = volunteer.email
                    td.text-center = volunteer.presence_count
